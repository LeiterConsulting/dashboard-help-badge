# Help Trigger

A 50-pixel click target that toggles a dashboard token through Dashboard Studio's supported drilldown API. Pair it with `dashboard_help_badge.help_panel` and a dashboard visibility condition.
