# Help Panel

A styled, token-aware companion panel for `dashboard_help_badge.help_trigger`. Use a Dashboard Studio visibility condition so the visualization container exists only while its paired token has the open value.
